syntax = "proto3";
package Protocol;

enum PlayerStateType
{
    PLAYER_IDLE    = 0;
    PLAYER_MOVE    = 1;
    PLAYER_DEAD    = 2;
    PLAYER_ESCAPE  = 3;
}

enum Role {
    ROLE_UNSPECIFIED = 0;
    ROLE_BOSS        = 1;
    ROLE_SURVIVOR    = 2;
}

enum EscapeType 
{
    ESCAPE_DEAD      = 0;         // 탈출 못함
    ESCAPE_EXIT_DOOR = 1;     // 탈출구 탈출
    ESCAPE_HATCH     = 2;       // 개구멍 탈출
}

// 에러 코드
enum ErrorCode {
    ERROR_NONE = 0;                 // 성공
    ERROR_INVALID_CREDENTIALS = 1;  // 잘못된 인증 정보
    ERROR_SERVER_FULL = 2;          // 서버 가득 참
    ERROR_UNKNOWN_ERROR = 99;       // 알 수 없는 에러
}

enum ActorRole {
    PLAYER = 0;         // 플레이어
    AI = 1;             // 인공지능
    ENVIRONMENT = 2;    // 환경 오브젝트
}

enum BossType {
    BOSS_NONE = 0;
    BOSS_PEANUT = 1;        //땅콩이
    BOSS_VOMIT = 2;         //토하는 여자
    BOSS_PROTEIN = 3;       //프로틴
}
